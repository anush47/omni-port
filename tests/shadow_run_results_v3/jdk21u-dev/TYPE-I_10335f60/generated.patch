diff --git a/src/java.desktop/share/classes/sun/print/CustomMediaSizeName.java b/src/java.desktop/share/classes/sun/print/CustomMediaSizeName.java
index 34035e5c1a3..824fda01063 100644
--- a/src/java.desktop/share/classes/sun/print/CustomMediaSizeName.java
+++ b/src/java.desktop/share/classes/sun/print/CustomMediaSizeName.java
@@ -27,15 +27,20 @@ package sun.print;
 
 import java.io.Serial;
 import java.util.ArrayList;
+import java.util.Map;
+import java.util.HashMap;
+import java.util.Objects;
 
 import javax.print.attribute.EnumSyntax;
 import javax.print.attribute.standard.Media;
 import javax.print.attribute.standard.MediaSize;
 import javax.print.attribute.standard.MediaSizeName;
+import javax.print.attribute.Size2DSyntax;
 
 class CustomMediaSizeName extends MediaSizeName {
     private static ArrayList<String> customStringTable = new ArrayList<>();
     private static ArrayList<MediaSizeName> customEnumTable = new ArrayList<>();
+    private static Map<SizeNameChoiceItem, CustomMediaSizeName> customMap = new HashMap<>();
     private String choiceName;
     private MediaSizeName mediaName;
 
@@ -99,7 +104,6 @@ class CustomMediaSizeName extends MediaSizeName {
         return choiceName;
     }
 
-
     /**
      * Returns matching standard MediaSizeName.
      */
@@ -107,7 +111,6 @@ class CustomMediaSizeName extends MediaSizeName {
         return mediaName;
     }
 
-
     // moved from RasterPrinterJob
     /**
      * Returns closest matching MediaSizeName among given array of Media
@@ -115,7 +118,6 @@ class CustomMediaSizeName extends MediaSizeName {
     public static MediaSizeName findMedia(Media[] media, float x, float y,
                                           int units) {
 
-
         if (x <= 0.0f || y <= 0.0f || units < 1) {
             throw new IllegalArgumentException("args must be +ve values");
         }
@@ -174,7 +176,6 @@ class CustomMediaSizeName extends MediaSizeName {
         return (Media[])super.getEnumValueTable();
     }
 
-
     /**
      * Returns the string table for class CustomMediaSizeName.
      */
@@ -191,4 +192,55 @@ class CustomMediaSizeName extends MediaSizeName {
       return customEnumTable.toArray(enumTable);
     }
 
+    public static CustomMediaSizeName create(String name, String choice,
+                                             float width, float length) {
+        SizeNameChoiceItem key = new SizeNameChoiceItem(name, choice, width, length);
+        CustomMediaSizeName value = customMap.get(key);
+        if (value == null) {
+            value = new CustomMediaSizeName(name, choice, width, length);
+            customMap.put(key, value);
+
+            // add this new custom media size name to MediaSize array
+            if ((width > 0.0) && (length > 0.0)) {
+                try {
+                    new MediaSize(width, length, Size2DSyntax.INCH, value);
+                } catch (IllegalArgumentException e) {
+                        /* PDF printer in Linux for Ledger paper causes
+                        "IllegalArgumentException: X dimension > Y dimension".
+                        We rotate based on IPP spec. */
+                    new MediaSize(length, width, Size2DSyntax.INCH, value);
+                }
+            }
+        }
+        return value;
+    }
+
+    private static class SizeNameChoiceItem {
+
+        private final String name;
+        private final String choice;
+        private final float width;
+        private final float length;
+
+        public SizeNameChoiceItem(String name, String choice, float width, float length) {
+            this.name = name;
+            this.choice = choice;
+            this.width = width;
+            this.length = length;
+        }
+
+        public boolean equals(Object object) {
+            if (this == object) return true;
+            if (object == null || getClass() != object.getClass()) return false;
+            SizeNameChoiceItem that = (SizeNameChoiceItem) object;
+            return Objects.equals(this.name, that.name)
+                    && Objects.equals(this.choice, that.choice) &&
+                    Float.compare(this.width, that.width) == 0 &&
+                    Float.compare(this.length, that.length) == 0;
+        }
+
+        public int hashCode() {
+            return Objects.hash(name, choice, width, length);
+        }
+    }
 }
diff --git a/src/java.desktop/share/classes/sun/print/CustomMediaTray.java b/src/java.desktop/share/classes/sun/print/CustomMediaTray.java
index 324059a56d6..36e70701179 100644
--- a/src/java.desktop/share/classes/sun/print/CustomMediaTray.java
+++ b/src/java.desktop/share/classes/sun/print/CustomMediaTray.java
@@ -27,6 +27,9 @@ package sun.print;
 
 import java.io.Serial;
 import java.util.ArrayList;
+import java.util.Objects;
+import java.util.Map;
+import java.util.HashMap;
 
 import javax.print.attribute.EnumSyntax;
 import javax.print.attribute.standard.Media;
@@ -35,6 +38,7 @@ import javax.print.attribute.standard.MediaTray;
 public class CustomMediaTray extends MediaTray {
     private static ArrayList<String> customStringTable = new ArrayList<>();
     private static ArrayList<MediaTray> customEnumTable = new ArrayList<>();
+    private static Map<NameChoiceItem, CustomMediaTray> customMap = new HashMap<>();
     private String choiceName;
 
     private CustomMediaTray(int x) {
@@ -47,7 +51,6 @@ public class CustomMediaTray extends MediaTray {
       return (customStringTable.size()-1);
     }
 
-
     public CustomMediaTray(String name, String choice) {
         super(nextValue(name));
         choiceName = choice;
@@ -60,7 +63,6 @@ public class CustomMediaTray extends MediaTray {
     @Serial
     private static final long serialVersionUID = 1019451298193987013L;
 
-
     /**
      * Returns the command string for this media tray.
      */
@@ -68,7 +70,6 @@ public class CustomMediaTray extends MediaTray {
         return choiceName;
     }
 
-
     /**
      * Returns the string table for super class MediaTray.
      */
@@ -76,7 +77,6 @@ public class CustomMediaTray extends MediaTray {
       return (Media[])super.getEnumValueTable();
     }
 
-
     /**
      * Returns the string table for class CustomMediaTray.
      */
@@ -93,4 +93,36 @@ public class CustomMediaTray extends MediaTray {
       return customEnumTable.toArray(enumTable);
     }
 
+    public static CustomMediaTray create(String name, String choice) {
+        NameChoiceItem key = new NameChoiceItem(name, choice);
+        CustomMediaTray value = customMap.get(key);
+        if (value == null) {
+            value = new CustomMediaTray(name, choice);
+            customMap.put(key, value);
+        }
+        return value;
+    }
+
+    private static class NameChoiceItem {
+
+        private final String name;
+        private final String choice;
+
+        public NameChoiceItem(String name, String choice) {
+            this.name = name;
+            this.choice = choice;
+        }
+
+        public boolean equals(Object object) {
+            if (this == object) return true;
+            if (object == null || getClass() != object.getClass()) return false;
+            NameChoiceItem that = (NameChoiceItem) object;
+            return Objects.equals(this.name, that.name)
+                    && Objects.equals(this.choice, that.choice);
+        }
+
+        public int hashCode() {
+            return Objects.hash(name, choice);
+        }
+    }
 }
diff --git a/src/java.desktop/unix/classes/sun/print/CUPSPrinter.java b/src/java.desktop/unix/classes/sun/print/CUPSPrinter.java
index e2a69c79c3e..1943c98f522 100644
--- a/src/java.desktop/unix/classes/sun/print/CUPSPrinter.java
+++ b/src/java.desktop/unix/classes/sun/print/CUPSPrinter.java
@@ -45,7 +45,6 @@ import javax.print.attribute.Attribute;
 import javax.print.attribute.EnumSyntax;
 import javax.print.attribute.standard.PrinterName;
 
-
 public class CUPSPrinter  {
     private static final String debugPrefix = "CUPSPrinter>> ";
     private static final double PRINTER_DPI = 72.0;
@@ -109,7 +108,6 @@ public class CUPSPrinter  {
         }
     }
 
-
     CUPSPrinter (String printerName) {
         if (printerName == null) {
             throw new IllegalArgumentException("null printer name");
@@ -147,7 +145,6 @@ public class CUPSPrinter  {
         }
     }
 
-
     /**
      * Returns array of MediaSizeNames derived from PPD.
      */
@@ -156,7 +153,6 @@ public class CUPSPrinter  {
         return cupsMediaSNames;
     }
 
-
     /**
      * Returns array of Custom MediaSizeNames derived from PPD.
      */
@@ -225,26 +221,13 @@ public class CUPSPrinter  {
             w = (float)(pageSizes[i*6+4]/PRINTER_DPI);
             y = (float)(pageSizes[i*6+5]/PRINTER_DPI);
 
-            msn = new CustomMediaSizeName(media[i*2], media[i*2+1],
-                                          width, length);
+            msn = CustomMediaSizeName.create(media[i*2], media[i*2+1],
+                                             width, length);
 
             // add to list of standard MediaSizeNames
             if ((cupsMediaSNames[i] = msn.getStandardMedia()) == null) {
                 // add custom if no matching standard media
                 cupsMediaSNames[i] = msn;
-
-                // add this new custom msn to MediaSize array
-                if ((width > 0.0) && (length > 0.0)) {
-                    try {
-                    new MediaSize(width, length,
-                                  Size2DSyntax.INCH, msn);
-                    } catch (IllegalArgumentException e) {
-                        /* PDF printer in Linux for Ledger paper causes
-                        "IllegalArgumentException: X dimension > Y dimension".
-                        We rotate based on IPP spec. */
-                        new MediaSize(length, width, Size2DSyntax.INCH, msn);
-                    }
-                }
             }
 
             // add to list of custom MediaSizeName
@@ -269,8 +252,8 @@ public class CUPSPrinter  {
 
         MediaTray mt;
         for (int i=0; i<nTrays; i++) {
-            mt = new CustomMediaTray(media[(nPageSizes+i)*2],
-                                     media[(nPageSizes+i)*2+1]);
+            mt = CustomMediaTray.create(media[(nPageSizes+i)*2],
+                                        media[(nPageSizes+i)*2+1]);
             cupsMediaTrays[i] = mt;
         }
 
@@ -360,7 +343,6 @@ public class CUPSPrinter  {
                          }
                     }
 
-
                     AttributeClass attribClass = defaultMap.get("printer-name");
 
                     if (attribClass != null) {
@@ -386,7 +368,6 @@ public class CUPSPrinter  {
         return null;
     }
 
-
     /**
      * Get list of all CUPS printers using IPP.
      */
@@ -506,7 +487,6 @@ public class CUPSPrinter  {
         return false;
     }
 
-
     /**
      * Detects if CUPS is running.
      */
@@ -527,5 +507,4 @@ public class CUPSPrinter  {
         }
     }
 
-
 }
diff --git a/src/java.desktop/unix/classes/sun/print/IPPPrintService.java b/src/java.desktop/unix/classes/sun/print/IPPPrintService.java
index 95a2682e058..35e28a59aac 100644
--- a/src/java.desktop/unix/classes/sun/print/IPPPrintService.java
+++ b/src/java.desktop/unix/classes/sun/print/IPPPrintService.java
@@ -151,7 +151,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
     private boolean gifImagesAdded = false;
     private boolean jpgImagesAdded = false;
 
-
     /**
      * IPP Status Codes
      */
@@ -180,7 +179,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
     // gets the list of printers
     public static final String OP_CUPS_GET_PRINTERS = "4002";
 
-
     /**
      * List of all PrintRequestAttributes.  This is used
      * for looping through all the IPP attribute name.
@@ -213,7 +211,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         Sides.ONE_SIDED,
     };
 
-
     /**
      * List of all PrintServiceAttributes.  This is used
      * for looping through all the IPP attribute name.
@@ -237,7 +234,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         {QueuedJobCount.class, "queued-job-count"}
     };
 
-
     /**
      * List of DocFlavors, grouped based on matching mime-type.
      * NOTE: For any change in the predefined DocFlavors, it must be reflected
@@ -345,7 +341,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         DocFlavor.URL.TEXT_HTML_HOST,
     };
 
-
     // PCL DocFlavors
     private static DocFlavor[] appPCL = {
         DocFlavor.BYTE_ARRAY.PCL,
@@ -361,7 +356,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         textHtml, appPCL,
     };
 
-
     IPPPrintService(String name, URL url) {
         if ((name == null) || (url == null)){
             throw new IllegalArgumentException("null uri or printer name");
@@ -391,7 +385,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     IPPPrintService(String name, String uriStr, boolean isCups) {
         if ((name == null) || (uriStr == null)){
             throw new IllegalArgumentException("null uri or printer name");
@@ -425,7 +418,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     /*
      * Initialize mediaSizeNames, mediaTrays and other attributes.
      * Media size/trays are initialized to non-null values, may be 0-length
@@ -499,7 +491,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     public DocPrintJob createPrintJob() {
         @SuppressWarnings("removal")
         SecurityManager security = System.getSecurityManager();
@@ -510,7 +501,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return new UnixPrintJob(this);
     }
 
-
     public synchronized Object
         getSupportedAttributeValues(Class<? extends Attribute> category,
                                     DocFlavor flavor,
@@ -845,7 +835,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     public AttributeSet getUnsupportedAttributes(DocFlavor flavor,
                                                  AttributeSet attributes) {
         if (flavor != null && !isDocFlavorSupported(flavor)) {
@@ -879,7 +868,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     public synchronized DocFlavor[] getSupportedDocFlavors() {
 
         if (supportedDocFlavors != null) {
@@ -975,7 +963,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return flavor;
     }
 
-
     public boolean isDocFlavorSupported(DocFlavor flavor) {
         if (supportedDocFlavors == null) {
             getSupportedDocFlavors();
@@ -990,7 +977,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return false;
     }
 
-
     /**
      * Finds matching CustomMediaSizeName of given media.
      */
@@ -1008,20 +994,19 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return null;
     }
 
-
     /**
      * Returns the matching standard Media using string comparison of names.
      */
     private Media getIPPMedia(String mediaName) {
-        CustomMediaSizeName sampleSize = new CustomMediaSizeName("sample", "",
-                                                                 0, 0);
+        CustomMediaSizeName sampleSize =
+                CustomMediaSizeName.create("sample", "", 0, 0);
         Media[] sizes = sampleSize.getSuperEnumTable();
         for (int i=0; i<sizes.length; i++) {
             if (mediaName.equals(""+sizes[i])) {
                 return sizes[i];
             }
         }
-        CustomMediaTray sampleTray = new CustomMediaTray("sample", "");
+        CustomMediaTray sampleTray = CustomMediaTray.create("sample", "");
         Media[] trays = sampleTray.getSuperEnumTable();
         for (int i=0; i<trays.length; i++) {
             if (mediaName.equals(""+trays[i])) {
@@ -1053,7 +1038,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return new Media[0];
     }
 
-
     public synchronized Class<?>[] getSupportedAttributeCategories() {
         if (supportedCats != null) {
             Class<?> [] copyCats = new Class<?>[supportedCats.length];
@@ -1124,7 +1108,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return copyCats;
     }
 
-
     public boolean
         isAttributeCategorySupported(Class<? extends Attribute> category)
     {
@@ -1225,7 +1208,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     public synchronized PrintServiceAttributeSet getAttributes() {
         if (!init) {
             // get all attributes for the first time.
@@ -1272,7 +1254,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return false;
     }
 
-
     private boolean isSupportedCopies(Copies copies) {
         CopiesSupported cs = (CopiesSupported)
             getSupportedAttributeValues(Copies.class, null, null);
@@ -1345,7 +1326,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
             return true;
     }
 
-
     public boolean isAttributeValueSupported(Attribute attr,
                                              DocFlavor flavor,
                                              AttributeSet attributes) {
@@ -1482,7 +1462,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return true;
     }
 
-
     public synchronized Object
         getDefaultAttributeValue(Class<? extends Attribute> category)
     {
@@ -1741,12 +1720,10 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return printer;
     }
 
-
     public boolean usesClass(Class<?> c) {
         return (c == sun.print.PSPrinterJob.class);
     }
 
-
     public static HttpURLConnection getIPPConnection(URL url) {
         HttpURLConnection connection;
         URLConnection urlc;
@@ -1766,7 +1743,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return connection;
     }
 
-
     public synchronized boolean isPostscript() {
         if (isPS == null) {
            isPS = Boolean.TRUE;
@@ -1799,7 +1775,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return isPS.booleanValue();
     }
 
-
     private void opGetAttributes() {
         try {
             debug_println(debugPrefix+"opGetAttributes myURI "+myURI+" myURL "+myURL);
@@ -1866,7 +1841,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         }
     }
 
-
     public static boolean writeIPPRequest(OutputStream os,
                                            String operCode,
                                            AttributeClass[] attCl) {
@@ -1918,7 +1892,6 @@ public class IPPPrintService implements PrintService, SunPrinterJobService {
         return true;
     }
 
-
     public static HashMap<String, AttributeClass>[] readIPPResponse(InputStream inputStream) {
 
         if (inputStream == null) {
